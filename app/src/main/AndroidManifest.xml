<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.nearbyplaces">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true">


        <provider
            android:name=".data.PlacesProvider"
            android:authorities="com.example.android.nearbyplaces" />

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.ViewNearByPlacesActivity"
            android:parentActivityName=".ui.MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ui.PlaceDetailsActivity"
            android:theme="@style/PlaceDetailActivityTheme"></activity>

        <receiver android:name=".widgets.PlaceWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/place_widget_info" />
        </receiver>
        <service android:name=".widgets.UpdateWidgetService"></service>
    </application>

</manifest>